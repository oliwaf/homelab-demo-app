replicaCount: 1

image:
  repository: ghcr.io/oliwaf/demo-app
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  className: nginx
  host: demo.lab.local
  path: /
  pathType: Prefix

resources: {}
